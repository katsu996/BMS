[project]
name = "bms-table-filter"
version = "0.1.0"
description = "beatoraja songdata.db / difficulty table filter & Pages viewer"
requires-python = ">=3.11"

[dependency-groups]
dev = ["ruff>=0.9.0"]
